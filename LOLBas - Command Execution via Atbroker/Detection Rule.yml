// Note: Always use the table name `DetectionTable` for your queries.
DetectionTable
| where ParentImage contains "C:\\Windows\\System32\\cmd.exe"
| where Image contains "C:\\Windows\\System32\\reg.exe"
